import { expect } from "chai";
import ProjectsGrid from "../src/components/ProjectsGrid";

describe("Projects Grid", () => {
    it("should sort projects on creation date", () => {
        expect("1").to.be.equal("1");
    })
})